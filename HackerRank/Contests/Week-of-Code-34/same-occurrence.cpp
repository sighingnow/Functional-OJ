#include <cstdio>
#include <cstdlib>
#include <iostream>

const int MAXN = 8000 + 5;

int ns[MAXN];

int solver(int n, int x, int y) {
    return nsubarrs;
}

int main() {
    int n, q, x, y;
    scanf("%d %d", &n, &q);
    for (int i = 0; i < n; ++i) {
        scanf("%d", ns+i);
    }
    while (q--) {
        scanf("%d %d", &x, &y);
        printf("%d\n", solver(n, x, y));
    }

    return 0;
}


