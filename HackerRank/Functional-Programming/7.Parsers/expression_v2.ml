(* HackerRank challenge - Functional Programming - 2-1 expressions v2 *)

let read_int _ = Scanf.scanf " %d" (fun x -> x)
let read_char _ = Scanf.scanf "%c" (fun x -> x)

let isdigit c = c >= '0' && c <= '9'
let isspace c = c = '\n' || c = '\r' || c = '\t' || c = ' '
let ischar c = c >= 'a' && c <= 'z' || c >= 'A' && c <= 'Z' 

tokenizer = match  with
    | '0'..'9'

parse_expression = 

parse_term = 

parse_factor = 




